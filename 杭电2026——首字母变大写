#include <stdio.h>
#include <ctype.h>
#include <string.h> 
int main()
{
	int i,len;
	char a[100];
	while(gets(a))
	{   
		a[0]=toupper(a[0]);
		for(i=1;a[i]!='\0';i++)
		{
			if(a[i-1]==' ')
			{
				a[i]=toupper(a[i]);
			}
		}
		printf("%s\n",a);
	
    }
}
