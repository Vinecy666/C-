#include <stdio.h>
#include <string.h>
int main()
{
	int n,i,t;
	char a[1000];
    scanf("%d",&n);
    getchar() ;
	while(n--)
	{   t=0;
		gets(a);
		for(i=0;i<strlen(a);i++)
		{
			if(a[i]<0)
			{
				t++;
			}
		}
		printf("%d\n",t/2);
	}
}
