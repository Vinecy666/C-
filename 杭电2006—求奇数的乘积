#include <stdio.h>  
int main()  
{  
    int n,i,x;
	
    while(scanf("%d",&n)!=EOF)
    {
    	int sum = 1;
    	for(i = 0;i<n;i++)
    	{
    		scanf("%d",&x);
    		if(x%2!=0)
    		{
    			sum = sum * x;
			}
		}
		printf("%d\n",sum);
	}
    return 0;  
}  
